<h1 class="d-flex justify-content-center">Cohort Dashboard</h1>


<h3 class="d-flex justify-content-center">Administrator</h1>
<h4 class="d-flex justify-content-center">THE NAME OF THE ADMINISTRATOR GOES HERE</h4>



<div class="container">
  <div class="row">
   <ul class="nav nav-tabs">
  <li class="nav-item">
    <p class="nav-link "> <%= link_to "Create", new_cohort_path %></p>
  </li>
  </ul>
  </div>
 </div>
<h1>Cohort Listing</h1>
<% if @cohort.blank? %>
  <p> There are no cohorts currently in the system.</p>
  <% else %>

 
  <p>There are <%=@cohort_size%> cohorts listed below:</p>
  <table> 
    <% @cohort.each do |cohort| %>
      <tr>
       <td><%= cohort.id%></td>
       
        <td><%= cohort.cohort_name %></td>
        <td><%= cohort.cohort_start_date %></td>
        <td><%= cohort.cohort_end_date %></td>
        <td><%= cohort.courses_id %></td>
        <td><%= cohort.teachers_id %></td>
        <%# <td><%= cohort.class_size%>
        <td><%= link_to 'Edit', edit_cohort_path(cohort.id) %></td>
        <td><%= link_to 'Destroy', cohort_path(cohort.id),
                method: :delete,
                data: { confirm: 'Are you sure?' } %></td><br>
      </tr>
    </table>
    <% end %>
<% end %>

<%# CohortStudent.where(cohort_id:17) %>
<table> 
    <%@costu = CohortStudent.all%>
    <%@costu.each do |new_class| %>
      <tr>
        <%# <td>Cohort-Name<%=  %></td> 
        <td>Cohort-ID: <%= new_class.cohort_id %></td>
        <td>Student-ID: <%= new_class.student_id %></td>  
        <td>Student-Name: <%=  new_class.student.first_name%> <%=  new_class.student.last_name%></td>
        <td>Student-Age: <%= new_class.student.age %></td>  
        <td>Student-Education: <%= new_class.student.education%></td> 
      </tr><br>
    <% end %>
</table>


